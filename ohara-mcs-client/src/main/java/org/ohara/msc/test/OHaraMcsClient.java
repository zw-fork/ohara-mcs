package org.ohara.msc.test;


public class OHaraMcsClient {

    // 参考linkClient.execute()，责任链，参数校验，签名校验（留空）、匹配对应的RPCClient，并将其初始化、异常封装、响应结果封装
    // OHaraMcsClient#request(body, options)

    // TODO 发请求，写入、读取、删除，前值校验
//    private GrpcClient rpcClient;
//    public GrpcClient grpcClient(OHaraMcsConfig config) {
//        GrpcClient client = (GrpcClient) SpiExtensionFactory.getExtension("grpc", RpcClient.class);
////        client.init(config);
//        return client;
//    }

}
